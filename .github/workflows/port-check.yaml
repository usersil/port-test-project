name: Argocd deployement

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Login to ArgoCD
      run: argocd login --username "${{ secrets.ARGOCD_USERNAME }}" --password "${{ secrets.ARGOCD_PASSWORD }}" --server http://a643e055e05314dfa8ab628ed545b9f7-783703239.us-east-1.elb.amazonaws.com

        # Add your build and test commands here
        echo "Building and testing..."
